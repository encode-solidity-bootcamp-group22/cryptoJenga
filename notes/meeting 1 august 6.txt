number of blocks as the period

dollar amount passed in placeBet as well as seed
using the commit and reveal pattern
the reason we need commit/reveal is to prevent miner manipulation of withholding a block if the random number generated by chainlink leads to an unfavorable result (ie granting reward to an opponent)